# Core dependencies
# connexion 2.14.2 requires werkzeug<2.3, which in turn pins Flask to 2.2.x.
# Both stay on those versions until the connexion 3 migration.
Flask<2.3.0,>=2.0.0
connexion[swagger-ui]==2.14.2
flask-cors==6.0.5

# Printer communication
brother-ql-inventree==1.3
Pillow==12.3.0
pysnmp==4.4.12
# pysnmp 4.4.12 breaks against pyasn1>=0.5 (removed pyasn1.compat.octets);
# pin to a compatible release so the optional SNMP import does not crash.
pyasn1==0.4.8
qrcode==7.4.2
# PDF rendering. pypdfium2 bundles the PDFium binary in its wheel (no system
# deps) and is Apache-2.0/BSD licensed.
pypdfium2==4.30.0

# Production WSGI server. Run with --workers 1 so the keep-alive
# singleton/thread stays in a single process (see docker-entrypoint.sh).
gunicorn==23.0.0

# OIDC token exchange. Was already present transitively (connexion pulls it in
# for the Swagger UI), but src/utils/oidc.py imports it directly, so declare it
# rather than rely on someone else's dependency staying put.
#
# A floor rather than a pin: the image already resolves a newer release, and
# pinning here would silently downgrade it on the next rebuild.
requests>=2.32.3

# Logging and error handling
structlog==23.1.0
